package cf.thdisstudio.audioDownloader;

import java.awt.*;

public class PlayerPanel extends Panel {
    public PlayerPanel(){
        setBackground(Color.decode("#545454"));
    }

    @Override
    public void paint(Graphics g) {
//        g.drawString("PlayList", (int) (getWidth()*0.1), (int) (getHeight()*0.1));
    }
}
